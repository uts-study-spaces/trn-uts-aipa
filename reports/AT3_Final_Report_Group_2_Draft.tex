\documentclass[11pt,a4paper]{article}

\usepackage[margin=2.2cm]{geometry}
\usepackage{fontspec}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{enumitem}
\usepackage{tabularx}
\usepackage{float}
\usepackage{titlesec}
\usepackage{xurl}
\usepackage{fvextra}
\usepackage{subcaption}

\setmainfont{Arial}
\graphicspath{{../report_assets/}{../results/}}
\hypersetup{colorlinks=true, linkcolor=blue, urlcolor=blue}
\setlist[itemize]{leftmargin=1.2em}
\setlist[enumerate]{leftmargin=1.4em}
\titleformat{\section}{\Large\bfseries}{\thesection}{0.6em}{}
\titleformat{\subsection}{\large\bfseries}{\thesubsection}{0.6em}{}
\DefineVerbatimEnvironment{verbatim}{Verbatim}{breaklines=true, breakanywhere=true, fontsize=\small}
\newcolumntype{Y}{>{\raggedright\arraybackslash}X}
\sloppy

\begin{document}
\begin{titlepage}
\centering
\vspace*{1.5cm}
{\Large Artificial Intelligence Group Assignment Report\par}
\vspace{1.3cm}
{\huge\bfseries Ticket Routing Intelligence\par}
\vspace{0.35cm}
{\large Explainable Multilingual Queue and Priority Prediction for Customer Support Triage\par}
\vspace{1.1cm}
{\large 36121 Artificial Intelligence Principles and Applications\par}
\vspace{0.35cm}
{\large AT3 -- Group 2\par}
\vspace{1cm}
\begin{tabularx}{0.82\linewidth}{Y r}
\toprule
\textbf{Group member} & \textbf{Student ID}\\
\midrule
Tumadhir Alsharhan & 25605852\\
Amal Maawadh & 25605864\\
Andres Ethorimn & 26029165\\
Laura Pacheco & 25940875\\
Mario Rubiano & 24900627\\
Carl Lazarus & 12043485\\
\bottomrule
\end{tabularx}
\vfill
\begin{tabularx}{0.82\linewidth}{>{\bfseries}p{0.28\linewidth} Y}
\toprule
Repository & \url{https://github.com/ETHLOB/trn-uts-aipa}\\
Prototype name & Ticket Routing Intelligence\\
Submission date & 16 May 2026\\
\bottomrule
\end{tabularx}
\vspace*{1cm}
\end{titlepage}

\section{Executive Summary}
\subsection{Overview of the Project}
Ticket Routing Intelligence is an AI decision-support system for customer support ticket triage. It analyses a ticket and returns predicted queue/category, priority, recommended team, escalation flag, summary, and explanation terms. The aim is not to replace agents, but to provide a structured first-pass recommendation for faster and more consistent review.

The implementation uses the Kaggle Multilingual Customer Support Tickets dataset. Five CSV files were audited; three compatible multilingual files were merged and two German-normalized files were excluded because they used a different queue taxonomy. After deduplication and filtering, the modelling dataset contains 44,275 usable tickets.

\subsection{Objectives}
The project objectives are:
\begin{itemize}
  \item predict support queue/category from ticket text and safe pre-resolution metadata;
  \item predict priority as high, medium, or low;
  \item compare explainable NLP classifiers using validation and test metrics;
  \item test whether a multi-layer neural network improves priority prediction;
  \item benchmark multilingual transformer embeddings as an alternative representation;
  \item add rule-based routing and escalation logic;
  \item provide human-readable summaries and explanation terms;
  \item present the solution through a Streamlit dashboard with single-ticket and batch-ticket analysis.
\end{itemize}

\subsection{Key Findings}
The selected model is TF-IDF with Linear SVM for both tasks. On the held-out test set, it reached macro F1 of 0.525 for queue/category and 0.568 for priority. Billing and Payments was the strongest category, with F1 around 0.779. High priority reached F1 around 0.624. These results support first-pass triage, not full automation, so human review remains necessary.

An optional transformer-embedding benchmark used multilingual MiniLM sentence embeddings and Logistic Regression. It reached sampled macro F1 of 0.235 for category and 0.350 for priority, below the full-data TF-IDF Linear SVM pipeline. This shows that a more complex representation is not automatically better without tuning, larger samples, and careful validation.

\section{Introduction}
\subsection{Background and Context}
Customer support teams receive many unstructured tickets about billing, product questions, access, outages, returns, security, and general requests. First-pass triage is important because it decides the queue, urgency, and review path. Manual triage can be slow and inconsistent, especially for multilingual tickets or urgent cases hidden in long text.

\subsection{Problem Statement}
The problem is slow and inconsistent ticket triage. A ticket must be mapped to a queue, assigned priority, checked for escalation risk, and routed to the correct team. If the first decision is wrong, customers wait longer and urgent cases may be missed. This project assists the decision while keeping humans responsible for final action.

\subsection{Significance and Impact}
The system can reduce manual work by giving agents an initial recommendation. It also improves transparency by showing explanation terms and escalation reasons. The prototype supports single-ticket analysis and batch upload from CSV or Excel files.

\subsection{Challenges and Constraints}
The main constraints are dataset imbalance, multilingual text, overlapping categories, and privacy. Some languages have fewer examples, and ticket text may include personal information. The summary function masks obvious emails and phone numbers. For reproducibility and privacy, the core prediction path uses simple explainable methods; the Gemini assistant is optional and separate from the classifier.

\subsection{Project Objectives and Scope}
The scope is a working prototype and reproducible pipeline with notebooks, Python modules, metrics, charts, and a Streamlit app. It is not production-ready. Production use would require ticketing-system integration, access control, monitoring, retraining, stronger privacy redaction, and testing on company data.

\section{Theoretical Justification}
\subsection{Overview of AI Methods Used}
The solution combines several AI methods:
\begin{itemize}
  \item supervised NLP classification for queue and priority prediction;
  \item TF-IDF vectorisation to convert text into sparse numerical features;
  \item Logistic Regression, Naive Bayes, and Linear SVM as comparison models;
  \item an MLP neural-network candidate for checking non-linear priority classification;
  \item rule-based reasoning for routing and escalation;
  \item reinforcement-learning routing exploration as a future extension;
  \item optional Gemini-based LLM assistance for conversational triage support;
  \item template-based summarisation with privacy masking;
  \item explanation terms from influential TF-IDF features;
  \item optional multilingual transformer embeddings for comparison evidence.
\end{itemize}

\subsection{Theoretical Foundations}
TF-IDF gives high weight to terms that are frequent in one document but less frequent across the corpus (Ramos, 2003). This is useful because words such as ``invoice'', ``refund'', ``outage'', or ``password'' can signal a queue. Linear SVM is suitable for sparse, high-dimensional text because it learns a separating boundary between classes (Cortes and Vapnik, 1995; Joachims, 1998). Logistic Regression provides a strong linear baseline, while Naive Bayes is a simple probabilistic baseline for text (McCallum and Nigam, 1998).

The rule-based layer represents operational knowledge. For example, words such as ``unauthorised'', ``fraud'', or ``breach'' can trigger security routing or escalation even when the predicted queue is different. This is structural knowledge representation: ML predicts labels, while rules encode business risk.

\subsection{Justification of Selected Methods}
Linear SVM was selected because it had the best validation macro F1 for both queue and priority. It is lightweight, fast, and explainable because the app can show influential TF-IDF terms. This is more suitable for the current prototype than a heavier model that is harder to run and explain.

The MLP candidate was tested to check whether a multi-layer model could improve priority prediction. It reached priority test macro F1 of 0.561, slightly below the Linear SVM result of 0.568, so it was not selected for the live app. The transformer benchmark was also kept separate from live prediction. It provides a semantic multilingual comparison using sentence embeddings (Reimers and Gurevych, 2019), but it used a sample of 2,500 tickets and performed below the current model. Therefore, MLP and transformer results are comparison evidence, not the deployed prediction path.

\subsection{Additional Paradigms and Experiment Rationale}
The transformer benchmark used multilingual MiniLM sentence embeddings and Logistic Regression for category and priority prediction. This experiment addresses SLO4 by testing a modern deep-learning representation related to transformer, Generative AI, and LLM methods. The codebase also includes MLP priority experimentation, reinforcement-learning routing exploration, and a Gemini AI Assistant. These components demonstrate broader AI paradigms, but the live prediction path keeps TF-IDF with Linear SVM because it is faster, reproducible, explainable, and stronger in the measured results.

\subsection{Related Work / Literature Support}
The project follows standard NLP classification practice: text normalisation, TF-IDF feature extraction, train/validation/test splitting, model comparison, and held-out evaluation. The responsible AI design follows human oversight, leakage control, transparency, and privacy minimisation (Ribeiro, Singh and Guestrin, 2016; Jobin, Ienca and Vayena, 2019).

\section{Workflow and Methodology}
\subsection{System Architecture Overview}
The workflow has seven stages: audit data, prepare text, build features, train models, evaluate results, route tickets, and explain outputs. Streamlit presents this workflow in the Overview tab and provides prediction tools in the Try Solution tab.

\begin{figure}[H]
\centering
\includegraphics[width=0.98\linewidth]{workflow_diagram_report.png}
\caption{Project workflow used in the final prototype.}
\end{figure}

\subsection{Data Collection and Preprocessing}
The dataset was downloaded from Kaggle. The compatible files were:
\begin{itemize}
  \item \texttt{aa\_dataset-tickets-multi-lang-5-2-50-version.csv}
  \item \texttt{dataset-tickets-multi-lang-4-20k.csv}
  \item \texttt{dataset-tickets-multi-lang3-4k.csv}
\end{itemize}
The two German-normalized files were audited but excluded because their queue taxonomy was different.

The pipeline normalises column names, combines subject and body into \texttt{ticket\_text}, removes duplicate subject-body pairs, and removes rows without usable text. It excludes \texttt{answer} because that field is written after the agent responds, so using it would create target leakage.

\subsection{Feature Engineering}
The main feature is \texttt{model\_text}, which combines cleaned ticket text with safe metadata tokens such as type, language, and tags. Cleaning lowercases text, masks emails and phone numbers, replaces long numbers, removes URLs, and keeps simple alphanumeric tokens. TF-IDF uses unigrams and bigrams with up to 30,000 features.

\subsection{Model Design and Algorithmic Approach}
The project trains two separate supervised tasks:
\begin{itemize}
  \item queue/category prediction using the dataset queue label;
  \item priority prediction using the dataset priority label.
\end{itemize}
For each task, Logistic Regression, Multinomial Naive Bayes, and Linear SVM were compared. The data was split into training, validation, and test sets. Models were selected using validation macro F1, retrained on training plus validation data, and evaluated on the held-out test set.

\subsection{Integration of AI Paradigms}
The final prototype integrates supervised learning, a probabilistic baseline, symbolic rules, explainability, template summarisation, MLP experimentation, reinforcement-learning routing exploration, transformer comparison, and optional LLM assistance. This shows multiple AI paradigms. Only the stable TF-IDF Linear SVM pipeline is used for live predictions.

\subsection{Design Decisions vs Implementation Details}
A key design decision was to prioritise reproducibility and explainability. The core prediction path does not call an external LLM; summaries are template-based and explanations use TF-IDF terms. This reduces cost, privacy risk, and dependency risk. The optional Gemini assistant is kept separate from the trained classifier, while transformer and MLP results are shown as comparison evidence rather than used for routing.

\section{Empirical Analysis and Results}
\subsection{Experimental Setup}
The final modelling frame contains 44,275 rows. The pipeline uses train/validation/test splitting with stratification when possible and a fixed random seed for reproducibility. Metrics are saved in \texttt{results/}; charts are saved in \texttt{report\_assets/}.

\subsection{Dataset Description}
The data includes English, German, Spanish, French, and Portuguese tickets. English and German dominate the dataset. Labels include Billing and Payments, Product Support, Technical Support, Customer Service, Service Outages and Maintenance, Returns and Exchanges, Sales and Pre-Sales, Human Resources, and General Inquiry.

\begin{figure}[H]
\centering
\begin{subfigure}{0.86\linewidth}
\centering
\includegraphics[width=\linewidth]{dataset_file_rows.png}
\caption{Rows contributed by each compatible Kaggle CSV file.}
\end{subfigure}
\vspace{0.5em}
\begin{subfigure}{0.86\linewidth}
\centering
\includegraphics[width=\linewidth]{language_distribution.png}
\caption{Ticket language distribution after preprocessing.}
\end{subfigure}
\caption{Dataset composition used by the final modelling pipeline.}
\end{figure}

\begin{figure}[H]
\centering
\begin{subfigure}{0.78\linewidth}
\centering
\includegraphics[width=\linewidth]{priority_distribution.png}
\caption{Priority distribution.}
\end{subfigure}
\vspace{0.5em}
\begin{subfigure}{0.86\linewidth}
\centering
\includegraphics[width=\linewidth]{queue_distribution.png}
\caption{Queue/category distribution.}
\end{subfigure}
\caption{Target-label distributions for priority and queue prediction.}
\end{figure}

\subsection{Evaluation Metrics}
The project reports accuracy, macro precision, macro recall, macro F1, and weighted F1. Macro F1 is the main selection metric because it gives equal importance to each class, which is important for imbalanced data.

\subsection{Results}
\begin{table}[H]
\centering
\small
\begin{tabularx}{\linewidth}{lYlrrrr}
\toprule
\textbf{Task} & \textbf{Model} & \textbf{Split} & \textbf{Accuracy} & \textbf{Macro P} & \textbf{Macro R} & \textbf{Macro F1}\\
\midrule
Category & Linear SVM & Test & 0.531 & 0.507 & 0.547 & 0.525\\
Priority & Linear SVM & Test & 0.581 & 0.567 & 0.569 & 0.568\\
Priority & MLP candidate & Test & 0.581 & 0.567 & 0.559 & 0.561\\
Category & Transformer + LR & Sampled & 0.272 & 0.249 & 0.293 & 0.235\\
Priority & Transformer + LR & Sampled & 0.357 & 0.359 & 0.359 & 0.350\\
\bottomrule
\end{tabularx}
\normalsize
\caption{Main test results and additional comparison evidence. The live app uses TF-IDF Linear SVM for both queue/category and priority.}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=0.94\linewidth]{model_comparison_macro_f1.png}
\caption{Validation and test macro F1 comparison for the main models.}
\end{figure}

\subsection{Transformer Benchmark Results}
The optional transformer benchmark used a reproducible sample of 2,500 tickets. Multilingual MiniLM embeddings were classified with Logistic Regression. Category reached 0.235 sampled macro F1 and priority reached 0.350, below the full-data TF-IDF Linear SVM results of 0.525 and 0.568.

\begin{table}[H]
\centering
\small
\begin{tabularx}{0.92\linewidth}{lYrr}
\toprule
\textbf{Task} & \textbf{Representation and classifier} & \textbf{Macro F1} & \textbf{Accuracy}\\
\midrule
Category & TF-IDF + Linear SVM, full test set & 0.525 & 0.531\\
Category & Multilingual MiniLM embeddings + Logistic Regression, sampled test & 0.235 & 0.272\\
Priority & TF-IDF + Linear SVM, full test set & 0.568 & 0.581\\
Priority & TF-IDF + SVD + MLP candidate, full test set & 0.561 & 0.581\\
Priority & Multilingual MiniLM embeddings + Logistic Regression, sampled test & 0.350 & 0.357\\
\bottomrule
\end{tabularx}
\normalsize
\caption{Direct report comparison between the deployed TF-IDF model and alternative priority/embedding experiments.}
\end{table}

This does not mean MLPs or transformers are unsuitable. It means the tested MLP and sampled transformer setup did not outperform the simpler pipeline. A stronger setup would need more data, class balancing, hyperparameter tuning, and possibly a better classifier. For this assignment, the result supports keeping Linear SVM for live prediction and discussing neural networks and transformers as validated future work.

\subsection{Result Interpretation and Discussion}
The results show useful predictive signal but also limitations. Billing and Payments is strongest because billing tickets have distinctive vocabulary. Service Outages and Maintenance also performs well. Product Support, IT Support, and Customer Service are harder because wording overlaps. Priority prediction is slightly stronger than category prediction, but low priority remains weaker than high and medium.

Language analysis shows a performance gap: English category macro F1 is 0.604, while German category macro F1 is 0.400. Future work should improve multilingual preprocessing, use more balanced data, or tune multilingual embeddings more carefully.

\begin{figure}[H]
\centering
\begin{subfigure}{0.82\linewidth}
\centering
\includegraphics[width=\linewidth]{per_language_macro_f1.png}
\caption{Macro F1 by language and task.}
\end{subfigure}
\vspace{0.5em}
\begin{subfigure}{0.82\linewidth}
\centering
\includegraphics[width=\linewidth]{per_class_f1_category.png}
\caption{Per-class category F1.}
\end{subfigure}
\caption{Detailed performance views used for critical reflection.}
\end{figure}

\section{Critical Reflection}
\subsection{Limitations of the Approach}
The main limitations are moderate macro F1, language imbalance, training/inference mismatch, and limited privacy masking. Training uses text plus safe metadata, while manually pasted tickets mainly provide text. Batch files can include richer columns. The app masks obvious emails and phone numbers, but it does not detect all personal information.

\subsection{Failure Cases}
Some tickets are ambiguous. A security issue may mention billing details and be predicted as Billing and Payments. The recommended team can still be Security Team because routing also uses security terms. This is useful, but it must be explained to users.

\subsection{Trade-offs}
The main trade-off is explainability versus complexity. TF-IDF Linear SVM is simple, fast, and explainable. Transformer embeddings can capture deeper semantic meaning, but they performed worse in the sampled benchmark and require heavier dependencies. LLM summarisation could produce richer text, but it adds privacy, cost, and reproducibility risks.

\subsection{Scalability and Computational Constraints}
The prototype runs locally with saved model files. Production use would need an API, authentication, logging, monitoring, drift detection, retraining, and ticketing-platform integration. Larger transformer experiments may need better hardware or cloud resources.

\subsection{Alternative Methods and Improvements}
Future work should include confidence calibration, active learning from agent corrections, Unicode-aware preprocessing, stronger PII redaction, SLA-aware escalation, and better transformer experiments. A text-only model could reduce the single-ticket training/inference mismatch.

\section{GitHub Repository}
\subsection{Repository Link}
The GitHub repository is:
\begin{center}
\url{https://github.com/ETHLOB/trn-uts-aipa}
\end{center}

\subsection{Instructions to Run the Project}
Create and install the environment:
\begin{verbatim}
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe -m pip install -e .
\end{verbatim}

Download the Kaggle files:
\begin{verbatim}
.\.venv\Scripts\python.exe -m customer_support_ai.download_data
\end{verbatim}

Train and evaluate the models:
\begin{verbatim}
.\.venv\Scripts\python.exe -m customer_support_ai.train
.\.venv\Scripts\python.exe -m customer_support_ai.report_assets
\end{verbatim}

Run the Streamlit app:
\begin{verbatim}
.\.venv\Scripts\streamlit.exe run app/streamlit_app.py
\end{verbatim}

Optional transformer benchmark:
\begin{verbatim}
.\.venv\Scripts\python.exe -m pip install -r requirements-transformer.txt
.\.venv\Scripts\python.exe -m customer_support_ai.transformer_benchmark --sample-size 2500
\end{verbatim}

\section{Conclusion}
\subsection{Summary of Findings}
This project built an AI decision-support prototype for customer support triage. It uses a real multilingual dataset, reproducible preprocessing, supervised model comparison, held-out evaluation, routing rules, escalation logic, explanation terms, and a Streamlit dashboard.

\subsection{Key Insights}
The strongest current approach is TF-IDF with Linear SVM. It gives moderate but useful performance and supports explainability. Results are strongest where categories have clear vocabulary and weaker where categories overlap or language representation is limited. The MLP candidate and transformer benchmark did not improve macro F1, so the final design keeps the simpler model for live predictions.

\subsection{Future Work}
Future work should improve multilingual handling, add confidence scores, learn from human corrections, strengthen privacy redaction, and test transformer, multi-layer network, reinforcement learning, or LLM-based methods under fairer conditions. Production deployment would also need monitoring, data governance, and support-system integration.

\section{Individual Contributions}
The table below summarises individual contributions using repository history, presentation artefacts, and supporting project documents.

\small
\begin{longtable}{>{\raggedright\arraybackslash}p{0.18\linewidth} >{\raggedright\arraybackslash}p{0.47\linewidth} >{\raggedright\arraybackslash}p{0.25\linewidth}}
\toprule
\textbf{Member} & \textbf{Specific contribution} & \textbf{Evidence}\\
\midrule
\endfirsthead
\toprule
\textbf{Member} & \textbf{Specific contribution} & \textbf{Evidence}\\
\midrule
\endhead
Tumadhir Alsharhan \newline ID: 25605852 & Led the main implementation direction and integration of the project. Built and refined notebooks 01--05, reusable Python modules, the Streamlit dashboard, README/run checklist, result files, charts, and demo examples. Coordinated dataset replacement, data-loading decisions, dashboard review, presentation material, and Q\&A preparation. & Notebooks 01--05; \texttt{src/customer\_support\_ai/}; Streamlit app; README; docs; results; report assets.\\
\addlinespace
Mario Rubiano \newline ID: 24900627 & Reviewed code, notebooks, app, outputs, and assessment requirements. Added executed notebook evidence and a status/gap report. Implemented the transformer benchmark, notebook 06, result files, dependency file, documentation updates, and Streamlit benchmark display. Prepared responsible AI and future-work content. & Notebook 06; transformer benchmark module/results; reports; responsible AI material.\\
\addlinespace
Andres Ethorimn \newline ID: 26029165 & Created and maintained the GitHub repository structure, managed access and main-branch integration, merged pull requests, cleaned large/sensitive files, restored result artefacts, reviewed integration changes, and supported empirical-results presentation. & GitHub history, repository maintenance, restored results, presentation artefacts.\\
\addlinespace
Amal Maawadh \newline ID: 25605864 & Supported project planning, implementation direction, report/presentation framing, and HD-alignment review. Added confidence-aware triage and reproducibility checks, including confidence guidance for human review. Drafted presentation content and notes, and contributed introduction/problem framing. & Confidence module/tests; reproducibility checks; project plan; presentation artefacts.\\
\addlinespace
Laura Pacheco \newline ID: 25940875 & Contributed to solution overview, dataset explanation, leakage-control discussion, and experimental extension work. Added a reinforcement-learning routing agent and supported discussion of multi-paradigm AI integration, including neural-network/RL ideas. & \texttt{rl\_routing\_agent.py}; presentation artefacts; dataset/leakage explanation.\\
\addlinespace
Carl Lazarus \newline ID: 12043485 & Contributed to the AI methods section, demo preparation, and user-facing assistant extension. Added the Gemini-based AI assistant module that orchestrates the trained ML pipeline through tool calls for ticket triage and draft-response support. & \texttt{llm\_agent.py}; Streamlit AI Assistant integration; demo/presentation artefacts.\\
\bottomrule
\end{longtable}
\normalsize

\section{References}
\begin{enumerate}
  \item Cortes, C. and Vapnik, V. (1995). Support-vector networks. \textit{Machine Learning}, 20, 273--297.
  \item Joachims, T. (1998). Text categorization with support vector machines: learning with many relevant features. \textit{ECML}.
  \item McCallum, A. and Nigam, K. (1998). A comparison of event models for Naive Bayes text classification. \textit{AAAI Workshop on Learning for Text Categorization}.
  \item Ramos, J. (2003). Using TF-IDF to determine word relevance in document queries. \textit{Proceedings of the First Instructional Conference on Machine Learning}.
  \item Reimers, N. and Gurevych, I. (2019). Sentence-BERT: Sentence embeddings using Siamese BERT-networks. \textit{EMNLP-IJCNLP}.
  \item Ribeiro, M. T., Singh, S. and Guestrin, C. (2016). Why should I trust you? Explaining the predictions of any classifier. \textit{KDD}.
  \item Jobin, A., Ienca, M. and Vayena, E. (2019). The global landscape of AI ethics guidelines. \textit{Nature Machine Intelligence}, 1, 389--399.
  \item scikit-learn developers. (2026). scikit-learn: Machine Learning in Python. \url{https://scikit-learn.org/}
  \item Streamlit developers. (2026). Streamlit documentation. \url{https://docs.streamlit.io/}
  \item Kaggle. (2026). Multilingual Customer Support Tickets dataset. \url{https://www.kaggle.com/datasets/tobiasbueck/multilingual-customer-support-tickets}
\end{enumerate}

\appendix
\section{Application Screenshots}
This appendix provides visual evidence of the Streamlit prototype and its main user-facing functions.

\begin{figure}[H]
\centering
\includegraphics[width=0.92\linewidth]{app_screenshots/01_overview_dashboard.png}
\caption{Overview dashboard with project summary, metrics, workflow, and evidence panels.}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.92\linewidth]{app_screenshots/03_single_ticket_result.png}
\caption{Single-ticket analysis showing queue, priority, recommended team, and escalation output.}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.92\linewidth]{app_screenshots/04_batch_upload.png}
\caption{Batch upload interface for CSV or Excel ticket files.}
\end{figure}

\end{document}
